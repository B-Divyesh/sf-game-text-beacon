[
  {
    "id": "sample-read",
    "claim": "Hear a sample objective right away",
    "where": "landing primary action and demo",
    "test": "npm run test:e2e -- --grep @claim:sample-read",
    "sandbox": "fresh demo route; shipped sample objective is ready to read"
  },
  {
    "id": "demo-isolated",
    "claim": "Demo — sample data, nothing is saved",
    "where": "demo banner",
    "test": "npm run test:e2e -- --grep @claim:demo-isolated",
    "sandbox": "fresh demo route; demo-only localStorage key prefix"
  },
  {
    "id": "local-demo-network",
    "claim": "The demo makes no third-party requests",
    "where": "privacy page and demo context",
    "test": "npm run test:e2e -- --grep @claim:local-demo-network",
    "sandbox": "demo shell assets resolve same-origin only"
  },
  {
    "id": "no-telemetry",
    "claim": "Beacon sends no telemetry",
    "where": "README and privacy page",
    "test": "npm run test:e2e -- --grep @claim:no-telemetry",
    "sandbox": "fresh browser context; landing to demo sample flow records only same-origin requests"
  },
  {
    "id": "no-payments",
    "claim": "Beacon has no payment flow",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:no-payments",
    "sandbox": "fresh browser context; complete sample flow has no account, checkout controls, or payment request"
  },
  {
    "id": "no-cloud-screenshots",
    "claim": "Beacon does not upload screenshots to a cloud service",
    "where": "landing limits and privacy page",
    "test": "npm run test:e2e -- --grep @claim:no-cloud-screenshots",
    "sandbox": "mocked local desktop bridge; opening the capture preview uses only the bridge and makes no third-party request"
  },
  {
    "id": "desktop-local-ocr",
    "claim": "The desktop capture uses local OCR and no cloud OCR endpoint",
    "where": "privacy page and README",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml claim_desktop_local_ocr",
    "sandbox": "native command runner invokes installed local Tesseract on a private capture fixture and contains no HTTP endpoint"
  },
  {
    "id": "free-no-account",
    "claim": "Free to use with no account",
    "where": "landing fact and privacy page",
    "test": "npm run test:e2e -- --grep @claim:free-no-account",
    "sandbox": "shipped client has no account or payment flow"
  },
  {
    "id": "saved-region-settings",
    "claim": "The desktop app saves the capture region and hotkey on your device",
    "where": "privacy page and desktop capture frame",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml claim_saved_region_settings",
    "sandbox": "fresh local settings directory; save a changed region and hotkey, reopen the store, and assert the exact persisted values"
  },
  {
    "id": "windowed-capture",
    "claim": "Beacon captures only the selected region from a windowed or borderless game",
    "where": "landing steps and desktop app",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml claim_windowed_capture",
    "sandbox": "a region extending beyond a local display is clamped to its exact visible bounds; an out-of-display region is rejected"
  },
  {
    "id": "capture-frame",
    "claim": "Choose the text region yourself with pointer or keyboard",
    "where": "landing facts and desktop capture frame",
    "test": "npm run test:e2e -- --grep @claim:capture-frame",
    "sandbox": "mocked local desktop bridge; pointer and keyboard draw, move, resize, then save a sample display frame"
  },
  {
    "id": "reading-queue",
    "claim": "Each result joins the speech queue in capture order; you can repeat or stop",
    "where": "landing steps and desktop reading queue",
    "test": "npm run test:e2e -- --grep @claim:reading-queue",
    "sandbox": "mocked local speech engine; dispatch two native captures and assert sequential speaks without cancellation"
  },
  {
    "id": "gamepad-read",
    "claim": "A connected controller's first button reads the saved frame once per press",
    "where": "desktop capture frame help and README",
    "test": "npm run test:e2e -- --grep @claim:gamepad-read",
    "sandbox": "mocked desktop bridge and controller; hold the first button and assert one capture and one visible queued reading"
  },
  {
    "id": "no-game-automation",
    "claim": "Beacon does not press game controls or change a game",
    "where": "landing preview, limits, and terms",
    "test": "npm run test:e2e -- --grep @claim:no-game-automation",
    "sandbox": "mocked desktop bridge; invoke Read this frame and assert that the only native operations are settings lookup and region capture"
  },
  {
    "id": "linux-ocr-package",
    "claim": "The Linux download installs local Tesseract OCR",
    "where": "desktop download status",
    "test": "npm run test:e2e -- --grep @claim:linux-ocr-package",
    "sandbox": "published-manifest fixture with both AppImage and Debian assets; assert the landing page selects the Debian package"
  }
]
